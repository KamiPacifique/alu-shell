#!/usr/bin/env bash
# This script lists files in the current directory and shows only the part after the first dash

for file in *; do
  # Only process non-hidden regular files that contain a dash
  if [[ -f "$file" && "$file" == *-* ]]; then
    echo "${file#*-}"
  fi
done
